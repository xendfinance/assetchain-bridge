import { ethers } from 'hardhat'

export const DEFAULT_LIMIT_PER_SEND = ethers.utils.parseEther('100')
export const DEFAULT_FEE_SEND = 0
export const DEFAULT_FEE_FULFILL = 0
export const DEFAULT_RELAYER_CONSENSUS_THRESHOLD = 1
